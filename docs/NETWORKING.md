# Networking

RubixLightingControl uses an existing router as the local network.

Internet connectivity is optional.

## Initial topology

```text
Phone / Tablet / Computer
          |
      Local Router
          |
   Raspberry Pi 4B
          |
      Local Router
          |
    Pico 2 W Nodes
          |
      LED Strips