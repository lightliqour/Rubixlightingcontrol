# Security

RubixLightingControl is designed primarily for local-network operation.

The system should not depend on cloud services for normal operation.

Authentication and remote-access hardening will be implemented before any public Internet exposure is recommended.

Do not expose a development installation directly to the public Internet.